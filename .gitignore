# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# TypeScript
*.js # If you only want to commit .ts files in src (and not compiled .js)
*.js.map
*.d.ts # Usually generated, but sometimes checked in for libraries
dist/ # Compiled JavaScript output directory

# IDEs and editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Operating System files
.DS_Store
Thumbs.db
ehthumbs.db

# Environment variables
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Docker (if you build images locally and don't want to commit context tarballs)
# Typically, Dockerfile and docker-compose.yml ARE versioned.
# But if you have other docker-related temporary files:
# .dockerignore is the primary way to control docker build context.

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage reports
coverage/
.nyc_output/

# Build artifacts (if any other specific ones)
build/

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.old

# TypeORM specific (if you generate diagrams or other temp files)
# migration-output/ (example if you output migrations to a non-versioned dir)

# Specific to this project (if any)
postgres_data/ # If this volume mount point accidentally gets created locally outside Docker volume management